<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:previewImage">@style/myImageView</item>
    </style>

    <style name="myImageView">
        <item name="android:layout_width">70dp</item>
        <item name="android:layout_height">70dp</item>
        <item name="android:padding">5dp</item>
    </style>

    <style name="myTextTitle">
        <item name="android:textSize">19sp</item>
        <item name="android:typeface">serif</item>
        <item name="android:fontFamily">serif</item>
        <item name="android:textColor">@color/textcolor_black</item>
    </style>

    <style name="myTextSubHeading">
        <item name="android:textSize">16sp</item>
        <item name="android:typeface">serif</item>
        <item name="android:fontFamily">serif</item>
        <item name="android:textColor">#757575</item>
    </style>


    <style name="AppTheme.Toolbar.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!-- Set proper title size -->

        <!-- Set title color -->
        <item name="android:textColor">@color/textColor_white</item>
    </style>
</resources>
